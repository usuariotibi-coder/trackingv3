query ObtenerProcesoEspecifico {
  procesoOpPorOperacionYProceso(
    operacionId: "3", cuando el empleado ve el qr se ve la operacion
    procesoId: "1"  cuando el empleado escanea se ve el proceso
  ) {
    id
    estado
    tiempoEstimado
    usuario {
      nombre
    }
    maquina {
      nombre
    }
  }
}

query ObtenerProcesoEspecifico($operacionId: ID!, $procesoId: ID!) {
      procesoOpPorOperacionYProceso(
        operacionId: $operacionId
        procesoId: $procesoId
      ) {
        id
        estado
        horaInicio
        horaFin
        tiempoRealCalculado
        tiempoEstimado
        maquina {
          id
          nombre
        }
        usuario {
          id
          nombre
        }
        proceso {
          id
          nombre
        }
        operacion {
          id
          operacion
          workorder {
            plano
            proyecto {
              proyecto
            }
          }
        }
      }
    }
  `;